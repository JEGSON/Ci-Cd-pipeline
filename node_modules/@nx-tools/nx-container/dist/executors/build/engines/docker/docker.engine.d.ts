import { ExecutorContext } from '@nx/devkit';
import { Inputs } from '../../context';
import { EngineAdapter } from '../engine-adapter';
export declare class Docker extends EngineAdapter {
    private standalone;
    private createdBuilder;
    private buildxVersion;
    getBuildxVersion(): string;
    getCommand(args: string[]): {
        command: string;
        args: string[];
    };
    initialize(inputs: Inputs, ctx?: ExecutorContext): Promise<void>;
    finalize(inputs: Inputs, _ctx?: ExecutorContext): Promise<void>;
    getImageID(): Promise<string | undefined>;
    getMetadata(): Promise<string | undefined>;
    getDigest(metadata: string): Promise<string | undefined>;
    getArgs(inputs: Inputs, defaultContext: string): Promise<string[]>;
    private getBuildArgs;
    private getCommonArgs;
}
//# sourceMappingURL=docker.engine.d.ts.map