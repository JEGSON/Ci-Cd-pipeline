import { RunnerContext as Context, RepoMetadata } from '@nx-tools/ci-context';
import { logger as L } from '@nx-tools/core';
import { Inputs } from './context';
export interface Version {
    main: string | undefined;
    partial: string[];
    latest: boolean | undefined;
}
export declare class Meta {
    readonly version: Version;
    private readonly inputs;
    private readonly context;
    private readonly repo;
    private readonly images;
    private readonly tags;
    private readonly flavor;
    private readonly date;
    readonly logger: typeof L;
    constructor(inputs: Inputs, context: Context, repo: RepoMetadata, _logger: typeof L);
    private getVersion;
    private procSchedule;
    private procSemver;
    private procPep440;
    private procMatch;
    private procRefBranch;
    private procRefTag;
    private procRefPr;
    private procEdge;
    private procRaw;
    private procSha;
    private static setVersion;
    static isRawStatement(pattern: string): boolean;
    private setValue;
    private setGlobalExp;
    private getImageNames;
    getTags(): Array<string>;
    getLabels(): Array<string>;
    getAnnotations(): Array<string>;
    private getOCIAnnotationsWithCustoms;
    getJSON(alevels: string[]): unknown;
    getBakeFile(kind: string): string;
    getBakeFileTagsLabels(): string;
    private generateBakeFile;
    private static sanitizeImageName;
    private static sanitizeTag;
}
//# sourceMappingURL=meta.d.ts.map